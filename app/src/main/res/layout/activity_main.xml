<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/pinkgrad"

    tools:context=".MainActivity">


    <include
        android:id="@+id/header"
        layout="@layout/activity_header"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />


    <Button
        android:id="@+id/generateStoryButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Générer l'histoire"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.498"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.446" />

    <Button
        android:id="@+id/view_saved_stories_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Histoire sauvegardée"
        app:layout_constraintTop_toBottomOf="@id/generateStoryButton"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.5"
        android:layout_marginTop="20dp" />
    <!-- Place le haut du bouton juste en dessous du bouton 'Générer l'histoire' -->
    <!-- Aligne le bouton avec le bord gauche du parent -->
    <!-- Aligne le bouton avec le bord droit du parent -->
    <!-- Centre le bouton horizontalement dans l'espace disponible -->
    <!-- Ajoute une marge de 20dp entre le bouton 'Histoire sauvegardée' -->

</androidx.constraintlayout.widget.ConstraintLayout>